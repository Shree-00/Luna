install 
requirements.txt
make a folder in engine name it "model" in model install- 
yolo8v8n.pt
blip
go to hugchat and used default model and import cookies.json add to engine
create a virtual environment envluna
